# CLAUDE.md

- Use `uv`. Don't use `pip`/`venv` directly.
- Use Linear MCP. Don't use Linear API directly.
- Create all issues in Lineal (user /to-linear-issues skill). Don't create them in Github
- For local code review (reviewing the current diff before pushing / opening a PR), use the `review-hard` skill — not the soft `code-review`. It's the project's default local reviewer.